// Function to set th JS marker
var setJSmarker = function() {
  document.documentElement.classList.remove("no-js");
  document.documentElement.classList.add("js");
}

// Set class to signal if JS is available or not
setJSmarker();
